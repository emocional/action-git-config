# action-git-config (deprecated)

This repository is **deprecated**. The composite action has been migrated to [emo-workflows](https://github.com/emocional/emo-workflows).

Use instead:

```yaml
uses: emocional/emo-workflows/.github/actions/git-config@main
```

Source: `.github/actions/git-config/action.yml` in `emo-workflows`.
